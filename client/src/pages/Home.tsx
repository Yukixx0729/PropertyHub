const Home = () => {
  return (
    <div className="container">
      <form>
        <input></input>
        <button>Filters</button>
        <button>Search</button>
      </form>
    </div>
  );
};

export default Home;
